import 'package:iconly/iconly.dart';
import 'package:flutter/material.dart';

class AppIcons {
  static const search = IconlyLight.search;
  static const grid = IconlyLight.category;
  static const wifi = Icons.wifi;
  static const signal = Icons.network_cell;
  static const battery = Icons.battery_full;
}
